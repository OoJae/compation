export function Disclaimer() {
  return (
    <footer className="relative z-10 border-t border-[rgba(255,255,255,0.07)]">
      <p className="mx-auto max-w-[1440px] px-[clamp(16px,3vw,32px)] py-4 font-mono text-[11px] leading-[1.6] text-mut4">
        Compation is tooling that executes the user&apos;s stated intent — not a custodian and not financial advice.
        The model reasons; a deterministic risk engine sizes every position.
      </p>
    </footer>
  );
}
